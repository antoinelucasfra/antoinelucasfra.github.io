[project]
name = "sync-keep"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "gkeepapi",
    "gpsoauth",
    "pyyaml",
    "trafilatura",
    "htmldate",
]

[tool.uv]
dev-dependencies = []
